<UserControl
    x:Class="WireFrame.Ruler"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WireFrame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xaml="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <xaml:CanvasControl x:Name="HorizontalCanvas" 
                        Height="{x:Bind RulerWidth, Mode=OneWay}"
                        ClearColor="{x:Bind BackgroundColor, Mode=OneWay}"
                        Draw="DrawHorizontalRuler"
                        Visibility="{Binding IsVerticalRuler, TargetNullValue=Collapsed}"/>

        <xaml:CanvasControl x:Name="VerticalCanvas" 
                        Width="{x:Bind RulerWidth, Mode=OneWay}"
                        ClearColor="{x:Bind BackgroundColor, Mode=OneWay}"
                        Draw="DrawVerticalRuler"
                        Visibility="{Binding IsVerticalRuler, TargetNullValue=Collapsed}"/>
    </Grid>
    
</UserControl>
