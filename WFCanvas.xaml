<UserControl
    x:Class="WireFrame.WFCanvas"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WireFrame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ScrollViewer x:Name="_scrollViewer" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" ZoomMode="Enabled" Background="Gray">

        <Grid x:Name="_grid" Width="{x:Bind Path=GridWidth, Mode=OneWay}" Height="{x:Bind Path=GridHeight, Mode=OneWay}" Background="DarkGray">

            <Canvas x:Name="_canvas" Width="{x:Bind Path=CanvasWidth, Mode=OneWay}" Height="{x:Bind Path=CanvasHeight, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White">

                <Ellipse x:Name="_ellipse" Canvas.Left="562" Canvas.Top="0" Stroke="Orange" Fill="LightSalmon" Width="562" Height="1218" />

                <Rectangle x:Name="_rectangle" Canvas.Left="0" Canvas.Top="0" Stroke="Pink" Fill="LightPink" Width="281" Height="609" />

            </Canvas>

        </Grid>

    </ScrollViewer>

</UserControl>
