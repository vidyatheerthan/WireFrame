<UserControl
    x:Class="WireFrame.Controls.WFRotationControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WireFrame.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Canvas x:Name="_canvas">
        <Ellipse x:Name="_outer_ring" Stroke="#FF011B8D" Fill="#33011B8D" StrokeThickness="1.0"/>

        <Path x:Name="_path" Fill="#C9011B8D" StrokeThickness="1.0">
            <Path.Data>
                <GeometryGroup>
                    <PathGeometry x:Name="_pathGeometry">
                        <PathFigure x:Name="_pathFigure" StartPoint="100, 50">
                            <ArcSegment x:Name="_arcSegment" IsLargeArc="True"
                                        Point="0, 100"
                                        Size="10, 10"
                                        SweepDirection="Clockwise" />
                            <LineSegment x:Name="_line1" Point="100, 50" />
                            <LineSegment x:Name="_line2" Point="200, 100" />
                        </PathFigure>
                    </PathGeometry>
                </GeometryGroup>
            </Path.Data>
        </Path>

        <Ellipse x:Name="_inner_ring" Fill="#FF011B8D"/>
        
    </Canvas>
    
</UserControl>
