<UserControl
    x:Class="WireFrame.Controls.WFShapeHighlight"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WireFrame.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Canvas>
        <Path 
            x:Name="_path"
            Canvas.Left="{x:Bind Left, Mode=OneWay}" 
            Canvas.Top="{x:Bind Top, Mode=OneWay}"
            Width="{x:Bind Length, Mode=OneWay}"
            Height="{x:Bind Breath, Mode=OneWay}"
            Stroke="Blue"
            StrokeThickness="3"
            Fill="#770000FF"
            Stretch="{x:Bind Stretch, Mode=OneWay}">
            <Path.Data>
                <GeometryGroup x:Name="_geometry_group" FillRule="Nonzero">
                </GeometryGroup>
            </Path.Data>
        </Path>
    </Canvas>
    
</UserControl>
