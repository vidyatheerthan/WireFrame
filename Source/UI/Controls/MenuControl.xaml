<UserControl
    x:Class="WireFrame.UI.Controls.MenuControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WireFrame.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Black">

            <TextBlock Text="Draw" Padding="5, 5" TextAlignment="Center" VerticalAlignment="Center" FontSize="16" />

        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="DarkGray">

            <TextBlock Text="Rect" Padding="5, 5" TextAlignment="Center" VerticalAlignment="Center" FontSize="16" PointerPressed="OnPointerPressedOnDrawRectangle" />

            <TextBlock Text="Ellipse" Padding="5, 5" TextAlignment="Center" VerticalAlignment="Center" FontSize="16" PointerPressed="OnPointerPressedOnDrawEllipse" />

        </StackPanel>

    </Grid>

</UserControl>
